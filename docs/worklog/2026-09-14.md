# 2026-09-14

Three releases, v0.94.287 → v0.94.289. The first two finished #843 (Mark
Purcell's ~1,500 kW achieved-battery-power report); the third opened #735's
long-deferred refactor. Also one real process failure, recorded below because
the mechanism that caused it is going to recur.

## #843 — finishing the unit fix (v0.94.287, v0.94.288)

v0.94.285 (13 Sep) fixed the *smear* half of #843: `resample_history_nearest()`
returning samples from the future, so one bad reading propagated across eight
hours. That left the reading itself. Mark chose option C — do both halves of
the costed A/B/C steer — and both shipped today.

**v0.94.287, option B: a physical sanity bound.** Any participant power sample
beyond `10 × max(max_charge_kw, max_discharge_kw)` is discarded, filtering raw
history *upstream* of the resampler so a bad value never enters a period mean.
The multiple sits deliberately in the middle of the three-orders-of-magnitude
gap between plausible hardware overshoot and a 1000x unit error: a 25 kW device
briefly reading 30 kW (1.2x) is kept, a household who configures `5.0` for a
real 25 kW charger (5x) keeps all their data, Mark's actual corrupt sample
(~60x) is discarded, and a full W-vs-kW error is caught with two orders of
magnitude to spare.

It **discards rather than clamps**, and that distinction matters more than it
looks. Clamping 1514 kW down to the 250 kW bound would assert a value that was
never measured and is still ~10x what the device can physically do — converting
an obviously-absurd number into a plausible-looking but still-wrong one, which
is strictly worse for a figure feeding a real cost calculation because it stops
tripping suspicion.

**v0.94.288, option A: per-row unit scaling.** The root cause was never the
magnitude, it was that `_kw_scale_factor()` reads a sensor's *current live*
`unit_of_measurement` exactly once and applies that single scale across a whole
day of history — structurally unable to see a sensor whose unit changes
mid-window. Mark pulled the raw history with attributes and found the exact
rows: his EV pack sensor reports `W` for ~80 seconds as the car wakes, then
`kW`. A live check hours later returned `kW`, so `1514.417` was scaled by 1.0.

New `fetch_entity_power_history_kw()` fetches **with** attributes on both the
native and REST paths and converts each row by its own recorded unit, mirroring
the thermal-history path's existing precedent rather than inventing a second
convention for the same problem. Where B *discards* an implausible sample, A
*corrects* it — the real 1.514417 kW reading survives into the reconstruction
instead of being thrown away.

**Deliberately one function with exactly one caller.** Preserving attributes
makes a recorder read materially heavier — a separate join, on a report that
fetches a full day per sensor per participant — so every other history read
keeps the cheap attribute-stripped path. That scoping is what made option A
affordable at all. `_kw_scale_factor()` is untouched and still correct for its
seven other callers.

26 new tests across the two, with Mark's four real rows verbatim. #843 closed.

**Devhub cannot validate either fix** and this was stated plainly on the issue
rather than papered over: devhub's `Test EV` uses synthetic sensors with no
sleep gap and never reproduced the failure. The real check is Mark re-running
`compute_quality_report` for 13 Sep on his own data.

## #735 stage 1 — solar input gathering extracted (v0.94.289)

`main()` had reached 1,735 lines and was still growing (1,646 when #735 was
filed three days earlier — and the 09-13 session contributed to the increase).
#735's own triage identified input gathering as ~67% of it and the safest third
to move: fetch-and-transform with clear inputs and clear outputs, no LP state,
no publish side effects.

Shipped the **solar slice only** — `build_solar_arrays()` in a new
`solver_inputs/` package — rather than all four input sources at once, so each
slice stays independently revertible. `main()` 1,735 → 1,344; `solver_writer.py`
13,880 → 13,510.

**The triage was wrong about which guardrail would bite.** It flagged the #357
anti-drift test as the thing to watch. That test compares `solver_writer.py`'s
own top-level `def` set against the docs copy, and moving *inline* code out of
`main()` doesn't change that set — it never fired. What fired instead were the
two **source-inspection** test files (9 tests), which read `solver_writer.py` as
*text* and grep it for the moved code. For stages 2-4: grep-the-source tests are
the real friction here, not the AST-based one.

**That friction is also where the payoff turned out to be.** Those tests are
inspection-style for an honest reason both docstrings state — the code was a
nested closure inside a 1,700-line function making live `ha_get`/`ha_post_state`
calls, unreachable end-to-end without mocking the whole solve cycle. Extracting
it removed that reason, so stage 1 added **15 real behavioural tests** that
assert the same properties by *running* the code: #115's no-source fallback,
drop-not-zero-fill, band widening on disagreement, the period-0-only live anchor
with per-sensor units (#453), and #546's auto-include dedup including its mirror
case. One was mutation-checked (flipping drop→zero-fill makes it fail) to
confirm it wasn't vacuous.

This is a better argument for continuing the refactor than the line count:
logic guarded by text-matching since it was written became executable tests in
one extraction. Each later stage should be scoped to do the same.

**"No behaviour change" was verified, not asserted.** A normalised diff of the
original 388-line block against the relocated code left **zero unexplained
differences** — the only deltas were the 53-line header comment becoming the
function docstring, three ruff reflows, and six signature changes where the
closures now take `grid_times`/`n_periods` explicitly. Plus: the repointed
inspection tests still pass with assertions otherwise unchanged, `main()` has no
orphaned references, and the full suite is green (2292 passed).

Live on devhub: `optimal` over 203 periods, period 0 solar = 4.524 kW against a
live PV sensor reading 4524 W — the moved anchor path scaling correctly and
applying to index 0 only.

**Known cost, carried forward.** `solver_inputs/` modules reach back into
`solver_writer` via a *deferred, in-function* import. Module-scope would be
circular, and — the less obvious half — the suite patches those helpers as
attributes on the `solver_writer` module object, so resolving at call time is
what keeps every one of those patches working. Both reasons dissolve once the
`solver/ha_bridge.py` step lands, which is an argument for **reordering** the
issue's own staging to do `ha_bridge` before stages 2-4 replicate the wart three
more times.

## The release-workflow encoding failure

v0.94.288's Release workflow failed and published nothing:
`UnicodeDecodeError: 'utf-8' codec can't decode byte 0x97 in position 636`.

Cause: a changelog-editing script used `pathlib`'s **default** encoding, which on
Windows is cp1252 — every em dash in the new entry was written as a bare `0x97`
byte, and the release job's UTF-8 read died on it.

Two things worth keeping:

- **The first repair attempt would have made it worse.** A blanket byte-level
  `replace(b'\x97', ...)` also hits *valid* UTF-8 continuation bytes elsewhere in
  a 500 KB file. The fix that worked was iterative — decode, catch, fix that one
  byte, repeat — and ultimately a rebuild from the `v0.94.287` tag's known-good
  copy with an explicit UTF-8 write, giving a +11-line diff and nothing else.
- **Every subsequent changelog edit now asserts before and after**: uniform line
  endings (this file is 100% CRLF, and writing LF would have produced a
  whole-file spurious diff), a unique anchor, and a successful UTF-8 decode of
  the result. The v0.94.289 entry caught two real problems this way before
  writing — a non-unique anchor, and the CRLF trap.

## A real process failure worth not repeating

Two agents were working this repo in parallel. Seeing v0.94.288's release job
fail, one of them fixed the encoding and shipped **v0.94.289** — but the other
had *already* fixed it via PR #854, and v0.94.288's release had since succeeded.
Worse, the v0.94.289 tag was pushed **before confirming its own PR had merged**,
so it landed on a commit where `manifest.json` still read `0.94.288`, and that
release correctly failed.

Cleanup: PR closed with an explanation, the dangling remote and local tags
deleted (they would otherwise have blocked a legitimate future v0.94.289 — which
is exactly what this file's own #735 entry above then needed).

Two rules out of it, both applied for the rest of the day:

1. **Re-check that a problem still exists before acting on it.** Parallel agents
   make stale premises very easy to acquire, and a failing CI job is a
   *timestamped* observation, not a current state.
2. **Never push a tag without first verifying the merge landed** — `git log` on
   `main` plus the manifest version, not an assumption that a green PR merged
   itself.

Both were followed for v0.94.289: merge confirmed on `main` at the right SHA
with `manifest.json` reading `0.94.289`, and the tag's availability re-checked
against both local and remote before creating it.

## Carried forward

- **#843's own root cause (1) is closed, but the pattern isn't.**
  `_kw_scale_factor()` still does a single live-state read for its seven other
  callers. None of them fetch per-row units. That is correct today only because
  no other caller reconstructs a long history window from a sensor that can
  change unit mid-window — worth re-checking rather than assuming if another
  participant-shaped reader is ever added.
- **#735 staging should probably be reordered** — see above.
- Still blocked on external input: #481 (replace the hard deadline vs add a soft
  band alongside), #768 (ship the thermal `commanded_state × rated_kW` proxy?),
  #452, and #495/#485 (both need an HA platform Nimbus doesn't have — `PLATFORMS`
  is `[SENSOR, NUMBER, SWITCH]`).

## #481 — the ambient-temperature-covariate half (v0.94.293), #768 merged same window

Mark's steer on #481, unblocking the piece of it that didn't need the soft-band
decision: "Use the soft band approach and wire in external temperature as a
covariate for the thermal models." Took the covariate half immediately (flagged
as "purely additive and clearly right" regardless of how the soft-band question
resolves); the hard-deadline-vs-soft-band question itself is still open, put back
to Mark rather than decided unilaterally — reversing #774's own hard guarantee
without an explicit answer would risk silently trading away the very thing five
real incidents (#726/#741/#733/#769/#770/#782) went into establishing.

**Verified against real data before writing any code**, per this repo's own
established discipline: 4 real idle segments across 4 days on
`weather.noosa_heads_hourly` / `water_heater.wwk302`. The ambient-scaled
coefficient (`k = decay_rate / (tank_mean - ambient_mean)`, Newton's law of
cooling) had a lower coefficient of variation than today's flat rate (59% vs
76%) — directional support at n=4, not proof, stated as such on the issue.

**`thermal_forecast.py`**: `learn_thermal_rates()` gained an optional
`ambient_history` parameter and a new `loss_coeff_per_h` output, guarded by a
`_MIN_AMBIENT_GAP_C` denominator floor (a tank near ambient temperature would
otherwise make `k` numerically explode — same reasoning
`_MIN_HEATING_SEGMENT_HOURS` already applies to short runs).
`project_temperature_forecast()` gained matching `loss_coeff_per_h`/
`ambient_forecast` parameters, applying the ambient-scaled decay only when both
are supplied; either omitted is byte-identical to before. Both old flat-rate
fields/behaviour stay the unconditional fallback — deliberately, so an install
with no weather sensor configured (the common case today) never regresses.

**Prior art recorded per #603's standing rule**: EMHASS's own `cooling_constant`
term does the identical gap-scaled thing (already surfaced in #774's own second
comment, which deferred it as "no ambient-forecast plumbing exists in this
codebase yet" — this PR is exactly that plumbing); HAEO has no equivalent
element.

**Wiring** (`solver_writer.py`): a new async recorder-history fetch for
learning (`_async_fetch_ambient_history()`, mirroring the existing thermal-
history fetch's own deadlock-safe pattern), and the existing
`weather.get_forecasts` fetch extracted out of `publish_weather_forecast_
mirrors()` into a shared `_fetch_weather_hourly_forecast()` helper for the
forward projection — both keyed off the hub's already-configured
`solver_weather_forecast_sensor`, no new wizard field. `thermal_loss_coeff_
per_h` persists on `LoadRunState` alongside its two siblings, relearned once
per calendar day the same way.

Scoped deliberately to `thermal_forecast.py`'s display/projection path only
(`kind=deferrable` loads) — does not touch #774's own LP `ThermalLoadConfig`
recurrence, so it doesn't change Mark's live HWS dispatch today (that load is
`kind=thermal`, using the LP's own solved `plan_temperature_forecast`
trajectory for display, not this projection).

27 new tests (`learn_thermal_rates()`'s ambient path and its denominator guard,
the shared `_ambient_value_at()` interpolation/extrapolation helper, and
`project_temperature_forecast()`'s ambient-aware decay including its
fallback/no-op cases), plus a `LoadRunState` round-trip test for the new
persisted field. Full local suite green throughout.

**PR #864 hit a real merge conflict on push** — #768 (Mark's own direct ask:
auto-discover a Controllable Load's power sensor from its device via the entity
registry, never by name) had merged to `main` in the interim and touched the
same `## [Unreleased]` CHANGELOG section. Resolved (kept both entries, #481's
above #768's dated section); `solver_writer.py`/`manifest.json` auto-merged
clean, no real overlap. Worth noting directly: #768 is what makes this PR's own
learning path (and the pre-existing flat-rate learning) actually run against
Mark's real HWS going forward — it had been running on `thermal_rates_source=
"fallback"` since #809 removed the `power_sensor` wizard field with no
equivalent default for a `water_heater`/`climate` entity.

CI green (7/7), no Claude Approvals check configured on this repo, no review
comments — merged as #864.

**A second same-day parallel-agent collision, this time on the release step
itself.** A different concurrently-running session cut and merged its own
`v0.94.293` release (PR #865) — identical rename-Unreleased/bump-manifest
content, tagged and pushed — about two minutes before this session's own
release PR (#866, opened without first re-checking whether #864 had already
been released) merged on top of it. The two PRs' changes were content-
identical, so the resulting merge was genuinely harmless (`git merge-tree`
showed no real conflict, `CHANGELOG.md`/`manifest.json` both landed correct
and non-duplicated on `main`) — but #866 itself was pure redundant work, and
pushing its own `v0.94.293` tag on top would have been a real problem (the tag
already existed, pointing at #865's own commit) had it not been caught before
tagging. No tag was pushed from this session.

This is the identical failure mode 09-13/09-14's own earlier entry already
named a rule for ("re-check that a problem still exists before acting on it")
— recurring anyway, because the check that would have caught it (fetching
`origin/main` and looking for an already-cut release before opening a new
release PR) wasn't actually performed here, only the *lesson* was remembered
in prose. The concrete gap: after a PR merges, check for a newer release
already covering it before assuming release-cutting is still this session's
own job to do.

Live devhub/household verification (per this repo's own release-validation
directive) is still outstanding as of this entry — #865's own PR body notes
the same devhub limitation flagged in 09-13's own carried-forward list
(`solver_weather_forecast_sensor` null, synthetic controllable loads there),
so devhub can only ever confirm the no-weather-sensor fallback path; real
confirmation of the ambient path itself needs Mark's own install. Neither
this session nor (as of this entry) #865's own session has HA-MCP access
confirmed to have completed that check yet — worth a direct look before
calling v0.94.293 done, not just merged.
