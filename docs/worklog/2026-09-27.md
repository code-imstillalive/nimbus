# 2026-09-27

Continues without a break from [2026-09-26](2026-09-26.md). The household was
away for the evening with one instruction — *"keep working... bring it all under
control"* — and came back briefly around 23:10 AEST to ask where four old issues
had gone.

Four PRs merged, one open, seven of Mark's IV&V findings closed, two epics
closed, and one issue of my own closed as wrong. Nothing deployed and no tag
cut; the reasoning for not cutting one is at the bottom and is the most useful
thing here.

---

## #937 stage 3 — the metric the issue is about now exists on every install

[PR #1307.] #937 measured naive persistence beating the ML load forecaster on 11
of 14 days (mean **-$0.71/day**) and named its own top action: *"more days."*
There were none and could not be — the day-ahead decomposition is assembled only
in the standalone cron writer, so the series stopped when the reference household
moved to the native path.

Stage 1 built the snapshot layer, stage 2 captured one per local day, and this
consumes it. Eleven attributes, including the three **attribution** terms (load
bias, load timing, solar) that `forecast_regret.py`'s own docstring says exist
because *"#937 assumes the load forecaster is the dominant term and nobody has
checked."* All five extra scenarios were already being computed and discarded, so
publishing them costs nothing the LP was not already paying.

Measured rather than estimated: **508 bytes**, 3.10% of the recorder's
16,384-byte cap, taking the projected remainder at full retention from ~49% to
~52%.

Deliberately "Part of", not "Fixes": the issue wants more *days*, and days come
from time. This removes the reason there were none.

## Mark's #1289 IV&V pass — all seven findings closed

[PRs #1307 and #1312.] Each had an `xfail(strict=True)` pinning test from
#1297; each is now un-xfailed and kept as a regression test.

**#1295's pinning test was the single CI failure on #1307**, which is the
mechanism working exactly as designed — a fix cannot land while leaving a test
claiming the bug is still present. Worth saying plainly, because that property
is what stops two people silently undoing each other's work.

Two findings were broader than reported:

- **#1293** — `number.py` carried **17** literals, not 16, and the codebase-wide
  grep the finding said should have been run turned up an **18th** in
  `solver_writer.py`'s offer-curve attributes. That one is fixed the *opposite*
  way: AEMO's Market Floor Price and Market Price Cap are Australian market
  constants, not the household's money, so resolving them against
  `hass.config.currency` would relabel a genuinely-AUD figure as EUR on a
  European install — a false statement about the value, not a localisation.
  Pinned to an explicit `AUD/kWh`. The CHANGELOG's *"zero hardcoded currency
  strings"* claim was mine and was false; it is corrected in place and now
  enforced by a test that greps the whole package.
- **#1291** — `calibration_weight_used` reached nothing. #1179 added it to
  `LPResult` and stopped, so its stated purpose (*"a field you read rather than
  an alignment you infer"*) was unmet. It now reaches `Plan` and the failure
  warning prints it **on both branches** — the success branch too, because
  without the healthy-cycle distribution a failing cycle's weight has nothing to
  compare against.

Two findings' own tests asserted only the positive half, so the other half was
verified directly before shipping: **#1296**'s escalation (a real gap must still
warn from cycle 2, and a good cycle must reset the count) and **#1294**'s silence
(a healthy install raises nothing, two orphans produce exactly one notification,
and the partial-load guard still bails first).

## Two measurement tools, because two report errors kept recurring

[PRs #1315 and #1316.] `tests/analyse_module_dependencies.py`.

**Dependency mode** answers what an extraction costs. Phase 2 of #1298:
**3,115 lines, 65 blocking dependencies**, and two things that were not obvious
before measuring — `_compute_report_for_window` belongs **last** (1,280 lines, 31
blockers, and it calls nine of the other seven's helpers), and the most-shared
blockers are recorder-access and config-resolution helpers, which are not
reporting concerns and so do not belong in `solver_reports/`. That is a
prerequisite PR, not part of the reporting move.

**Caller mode** answers Mark's #1298 question — whether the compatibility façade
requirement should stand — and the phases disagree:

```
--callers --phase 2   2 production callers, 11 monkeypatch sites  -> facade IS load-bearing
--callers --phase 3   0 and 0                                     -> facade preserves nothing
--callers --phase 6   0 and 0                                     -> same
```

Phase 2's two callers are `services.py`'s `compute_quality_report` and
`rescore_history` handlers — a documented public surface, not an unused internal
one. And `services.py` resolves them as **module attributes at call time**, so a
re-export preserves both the handlers and all 11 monkeypatch sites. Migrating the
caller instead would make those patches land on an attribute nobody reads: the
tests would pass having exercised the real scorer. Green CI, no coverage.

So the criterion worth recording is not *"is a façade required"* but **"does
anything outside the module resolve this name at call time"**, which each phase
can now answer for itself.

## #1290 — earning the carve-out the fix shipped without

[PR #1317, open.] Reading `docs/release-process.md` before cutting a tag stopped
the tag.

#1290 changes how an all-day calendar event is read — UTC midnight to LOCAL
midnight — which moves an EV departure deadline by the whole UTC offset, ten
hours here. That is the one category the policy holds overnight. There is a
carve-out for a change that cannot affect an install which has not opted in, but
it requires *a test in the same PR demonstrating that*, and says explicitly that
asserting it in the PR description does not qualify.

**#1312 shipped it without that test.** The gating is real — one caller, behind a
trip-calendar guard — but "the gating is real" is exactly the claim the policy
rejects. #1317 closes the gap.

**The positive control is the part worth keeping.** A test asserting an opted-in
participant *does* reach the call, because without it "never called" is vacuous.
It failed twice for two different real reasons: the fixture lacked a SoC sensor
(participant skipped before the guard, with a warning nobody would have read) and
the stub lacked `safe_num` (raised before the guard). Both would have left the
negative tests green while proving nothing.

It also replaced a first draft of that file whose "behavioural" half asserted
that a fixture lacking a key lacks it, and that `bool("")` is `False` — testing
Python, not this codebase, while its docstring claimed to count real calls.

## #1313 — filed by me, wrong, and closed the same night

I filed an issue claiming `select.py` shadows the stdlib `select` module, so a
bare `python tests/test_X.py` cannot import `solver_writer`.

**#1240 had already diagnosed, measured and fixed that hours earlier**, and it is
written up in [2026-09-26](2026-09-26.md) under a heading naming the problem
directly. I had not read the current day's own worklog before filing.

Worse, the evidence I filed was self-inflicted. My scratch runner did:

```
PYTHONPATH="custom_components/nimbus_load;tests" python tests/test_X.py
```

which front-loads the shadowing directory — reintroducing in my own harness
exactly the condition #1240 removed from the repo. On clean `main`,
`_solver_path.py` appends, and the bare-run failure is a plain
`ModuleNotFoundError`, not a circular import.

I had also written in a PR body that this was *"the real cause of
`test_local_hour_is_used_everywhere.py`'s local errors, which an earlier session
wrote off as merely environmental"* — while being that later session,
re-diagnosing a fixed bug from a symptom I had produced myself.

**Rule taken from it: read this repo's worklog for the current day before
filing.**

## Closed with measurements rather than code

- **#476** (loads spec epic) — all ten deliverables resolved. Two claims in its
  own body were stale and are corrected: `solver_writer.py` passes real
  subentry-derived `sheddable_loads=`/`adequacy_loads=` on every live solve, so
  *"none of them is reachable from a real install"* and *"the config surface is
  greenfield"* are both false. The one genuinely undecided piece — whether a
  household mode may change **schedule** levers, not just quantities — is now
  **#1314**, because `ThermalLoadConfig` is a hard LP constraint by choice
  (#774's five incidents) and making it conditional on a `select` is a
  dispatch-semantics decision, not a table entry.
- **#1289** — head issue, with the seven-finding summary.
- **#1247** — the SoC-discrepancy disagreement was a **cross-version
  comparison**, not a disagreement about mechanism. v0.94.417, which that report
  states it ran, contains neither #1228 nor #1181 (`#1228` refs: 0,
  `soc_discrepancy_power_coverage`: 0); v0.94.422 has both (4 and 3). A
  mean-vs-point artifact concentrates on the steepest ramp **by construction**,
  so pre-fix and post-fix readings of the same day cannot put the maximum in the
  same hour. The better question replaces it: if the peak stays in the flat
  02:00–06:00 window after a deploy, that is a *stronger* #1181 finding, because
  a flat window has no ramp to blame.
- **#496** — interim check at 3h45m of its 24h window. Headroom does leave zero
  (`grid_export_headroom_kw` reached 19.069 kW), but the criterion *"do they ever
  leave 0.0"* treats 0.0 as a failure signal when it currently means three
  things: genuinely zero ranging width, no ranging entry for that variable, or
  `degenerate` (#490's clamped zero-width convention). `Headroom.degenerate` is
  computed and surfaced for per-load signals and **not** for grid headroom.
  I nearly filed the import/export asymmetry as a defect and it would have been
  wrong: headroom is HiGHS bound ranging, not `limit − planned`, and
  `forced_import_cost 0.5393` against `forced_export_cost −0.0087` is consistent
  with import pinned at a bound.

## Why no tag was cut

Three things want a release from current `main`, and none is in any tag:

- **#1179** is deploy-blocked on work v0.94.425 does not contain. Its
  instrumentation was half-built — production logs *whether* a failing solve took
  the minimum-weight fallback (#1229) but not the weight, because #1179's own
  field never reached the warning. That was #1291. Verified:
  `git show v0.94.425:…/solver_writer.py | grep -c weight_note` → **0**.
- **#1270**'s decisive test needs #1294's notification, which devhub (v0.94.422)
  does not have.
- **#937 stage 3** produces nothing until a scored day has a snapshot captured
  before it started.

It was 23:00–02:00 AEST throughout — inside the P2P window for the first hour —
and a deploy is the household's own action regardless. Cutting a tag is not, but
#1317 is the evidence that makes the unreleased range policy-clean, and it should
land first.
