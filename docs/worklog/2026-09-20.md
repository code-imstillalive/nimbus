# 2026-09-20

One release (**v0.94.396**), two of Mark Purcell's IV&V findings fixed and closed
(#1140, #1141), and part 3 of #1120 built. The household is still travelling; this
was the first session picking the backlog up cold after the 2026-09-18 handover.

**The day's shape: four of its most useful results were corrections to this
session's own work, none of which reached a release.** All four were
the same species — a claim that looked measured and was an artifact of how it was
looked at — which is the species this worklog keeps recording. The third was made
inside the paragraph describing the first two; the fourth was caught by a guard
rather than by me.

---

## v0.94.396 — two IV&V fixes

Mark's PR #1142 merged that morning carrying `xfail(strict=True)` pinning tests for
#1140 and #1141, both still open. That is a deliberate handshake: fixing the bug
makes the test XPASS and **fails CI** until the marker comes off. Both markers are
now gone, so the tests guard the fixes instead of documenting the defects.

**#1140 — the shared-charger widening ignored discharge.**
`_widen_shared_charger_cap_to_achieved()` exists because of #956: narrowing the
scorer's oracle below the trajectory that genuinely happened sends regret negative
and makes the comparison invalid. It computed "the largest simultaneous draw" from
`charge_kw` alone. The live constraint it widens against sums
`charge_vars[m][t] + discharge_vars[m][t]` into one ceiling — physically right,
since one connector's throughput is bounded regardless of direction. So a V2H
participant discharging through the shared charger was **invisible to the
widening**, and the cap could stay narrower than the real combined draw,
reintroducing #956's failure for the discharge case specifically.

Untested, too: the existing pinning test only ever passed zero for discharge. And
the docstring claimed the function measured "the largest simultaneous draw the
group actually made" — what it *should* do, not what it did. Corrected alongside
rather than left for the next reader to re-derive.

**#1141 — the flex signals parent's payload never reached diagnostics.**
`_flex_diagnostics()` never called `hass.states.get()` on
`sensor.nimbus_flex_signals` at all, on the strength of a docstring stating that
entity "carries no payload attributes at all". It does:
`publish_flex_signals()` posts ten scalars (all flattened) **plus**
`battery_signals`, `load_signals` and `generated_at`, none of which are flattened
anywhere. All three were silently absent from every diagnostics download — the
per-battery and per-load breakdown, and the timestamp saying when the snapshot was
taken, which is most of what someone downloading diagnostics to debug flexibility
actually wants.

#116's failure class again, relocated to the **seam** between the two mechanisms
rather than living inside either one.

Fixed by spreading the parent's attributes minus those that already have a
flattened child. **The exclusion is derived from `FLATTENED_ATTRS_FLEX` rather
than hand-listed** — that is the entire point, since a curated allowlist here is
precisely what drifted in #116. Add a flattened row and it drops out for free; add
a parent-only field and it appears for free. The payload is spread *before* the
structural keys, so a future colliding attribute name can never shadow them.

### The wrong docstring claim was not careless, and that is the interesting part

Measured on the dev install, "carries no payload attributes at all" is **true**.
That entity is `unknown` there with only metadata attributes, because flex never
populates on that install. The error was generalising one install's state into a
claim about the code.

Which is also why neither fix could earn a live verification, and the CHANGELOG
says so rather than papering over it: the dev install has no shared-charger group
that discharges, and no flex payload to spread. "Devhub validation: not claimed,
because X" is a first-class answer and was the honest one twice today.

One small thing fell out of the #1141 fix that is worth keeping: the dump now
carries `signals_entity_found` and `signals_state`. On an install where flex never
populates, `signals_state: "unknown"` **self-documents** exactly the situation that
produced the wrong docstring. Had that been in the dump, the claim would have been
visibly install-specific rather than looking like a fact about the code.

---

## Four corrections to this session's own work

**A version bump bundled into a feature branch.** The fix PR was opened carrying
`CHANGELOG.md` and `manifest.json` alongside the source. Checking #1134 against
#1135 showed the real convention — fix PRs carry source and tests only; CHANGELOG
and manifest land in their own Release PR. That bundling is precisely what blocked
the release queue on 2026-09-16: two branches each carrying a bump, neither
tagged, so no tag from `main` was clean in either direction. Split before merging.

Worth stating plainly because CI was already green on the bundled revision. Green
CI is not evidence of convention.

**A verification that verified nothing.** After deploying, the new service was
checked with a template asking `{{ "nimbus_load.rescore_history" is defined }}`.
That returns `True` for any string literal — and the service is not in v0.94.396
at all, it is still on an unmerged branch. A meaningless test returning the
expected answer. Caught before it was reported as a pass, but only just, and the
lesson is the same one #1141's docstring teaches: **a check that cannot fail is
indistinguishable from a check that works** (#757's rule, third recurrence this
month by my count).

The real marker was the one already in hand — the dev install's own
`nimbus_version` attribute reading `0.94.396`.

**And a third, made while writing this entry.** Updating the "Recent history"
index, its header was found claiming "Most recent 5" while listing more than
that. The correction written was "while carrying eight" — read off a
`grep -c` piped through `head -8`. The real count is **fourteen**. The pipeline
truncated the output and the truncation was read as the measurement.

Three for three, same species: a number or a check that *looked* measured and was
an artifact of how it was looked at. The two that shipped nothing were caught by
habit; this one was caught only because the corrected line was re-read against a
fresh count. Worth stating because the first two arrived with a plausible story
attached and this one arrived inside the very paragraph describing the pattern.

**And a fourth, which CI caught rather than I did.** #1120 part 3 was pushed after
running only the targeted tests — the full local suite had been started, found
unreliable because a rebase moved files under it mid-run, killed, and then not
restarted. Two guards failed on CI, both legitimately:

- `test_solver_writer_no_silent_failures` caught the new `except` handler
  appending its skip reason to the **return value** while never logging it.
  Returning the reason satisfies the caller and leaves nothing in the log for
  anyone reading the install afterwards — the exact silent-failure shape that
  guard exists to stop. Now logged *and* returned.
- The README services list requires every service to be documented, and the new
  one was not.

The reasoning that produced this was "CI runs the same suite and is authoritative,
so the local run is redundant" — true in isolation, and wrong as a decision,
because it made a single signal the only signal. Both gates now run in parallel.

Four for four, and the honest ranking is that **the repo's own instrumentation
outperformed self-checking today**: three were caught by habit, one by a guard,
and none by the suite that was skipped. That is an argument for the guards rather
than a complaint about them — every one of them was written after a defect of
exactly the shape it now catches.

---

## #1120 part 3 — `nimbus_load.rescore_history`

That issue's own last comment calls this "the only substantive item left"; parts 1
and 2 shipped earlier (the per-row version stamp in v0.94.392, and the trend
card's axis clipping).

A day is scored **once**, the morning after, and frozen. Nothing recomputes an
entry, so every scoring-formula change splits the table into two incomparable
halves on the same card. `compute_quality_report()` already scores an arbitrary
window correctly — it *returns* the answer and writes nothing back, so an operator
who knows exactly which row is stale still has no way to fix it.

**A separate service rather than a `persist: true` flag**, which is the choice the
issue leaves open. `compute_quality_report` scores an **arbitrary window**; this
table is keyed by **calendar day**. Persisting a 6-hour or 3-day window would mean
either silently rounding it to a day key — wrong, and invisibly so — or rejecting
most calls. Whole days are the only unit the table can hold.

Design points that each earned a test: day boundaries derived exactly as the daily
scorer derives them (local midnight to local midnight, `allow_partial=False`), so
a rescored row is comparable to an ordinary one; a failed day skipped with its
reason rather than aborting a run in which every prior day cost a MILP; rows
written through `_carry_forward_quality_history()` so a rescored row is stamped
with the release that produced it, preserving part 1; and — the subtle one — if
the rescored day **is** the published day, the headline attributes and state
channel move too, because the "Yesterday" card reads top-level attributes while
the trend card reads `history`, and correcting one without the other *is* the
defect #1120 describes.

**Two of the thirteen new tests were mutation-checked rather than trusted.**
Dropping the headline sync, and letting a raising day abort the run, each fail
exactly the test written for them and restore green. Writing a test that passes
first try is when to check it discriminates.

**The #357 function-set drift guard caught the new public function, correctly.**
It is listed in `INTENTIONAL_NATIVE_ONLY` beside its four quality siblings,
because the docs copy compared against is the **forecast** writer, which carries
no quality machinery at all — quality has its own separate script. The annotation
deliberately does **not** claim the body is native-only in principle, since it
isn't, and says to re-examine if the quality script ever becomes the comparison
target. A factually false annotation there is what that guard's own docstring
warns against.

---

## A reusable operational finding

The dev install's MCP endpoint speaks ordinary HTTP JSON-RPC, so its full tool
surface is reachable from a plain HTTP client even when the client's own MCP
integration is not loaded for that session. Worth knowing before concluding a
session cannot reach that environment: the transport is not the integration.

Two things re-confirmed there rather than assumed, both already recorded but both
easy to get backwards:

- The canonical `sensor.nimbus_*` ids on that install still resolve to the
  **production** instance's data. Its own entities are the `_2`-suffixed ones.
- **That duplication is per-entity, not per-install.** The quality report has a
  `_2` sibling; the battery forecast does not. So its own quality version is
  readable and its own solve status is not — and generalising from the one that
  worked is exactly the error made on 2026-09-17 in both directions within an
  hour.

---

## Later the same day — v0.94.397 verified, v0.94.398 shipped

Three more releases-worth of activity after the section above: #1120 closed on a real
verification, two of Mark Purcell's day-ahead reports answered, and one new finding
measured, filed, built and shipped.

---

## Answering two day-ahead reports that had no reply

#1138 (19 Sep) and #1143 (20 Sep) both sat with **zero comments**. Mark files these
daily; leaving two unanswered is its own kind of failure regardless of whether either
contained a defect. Both were clean and self-explained — his own judgement of "no new
findings" held up on re-reading.

So rather than only acknowledge them, the reference household's own 19 Sep was scored to
put a second install's numbers against his for the identical date. Same reliability split
on both: `regret_reliable` true while `epr_reliable` is false via the known #949
fleet-blend gap. Two installs, different retailers, different hardware, same day, same
shape.

### The day's regret was one decision, and the hourly table hides that

```
hour 11   -6.17     <- largest magnitude of the day, and NEGATIVE
hour 12   +0.82
hour 13   +3.84
hour 14   +4.84     <- what "rank the top hours" points at
hour 15   +1.94

gross positive +11.46   gross negative -7.81   net +3.65
```

Summed rather than ranked:

```
                   achieved   oracle    delta
charge_kwh          108.12     83.14   +24.98
grid_import_kwh      60.01     39.83   +20.18
evening discharge    92.22     88.75    +3.47
```

Achieved bought ~25 kWh more than the oracle inside the flat-price midday window and
returned ~3.5 kWh of it to the evening; the rest left at the 5c export rate. The hourly
split into one -$6.17 hour and four positive ones is because the oracle takes its charge
in a single hour at max power while achieved spreads it over five — same joint decision,
different shape.

**The cancellation itself is not a discovery and was not presented as one.**
`hourly_regret_breakdown()`'s own docstring already says a day "can have a small net
regret while still containing large, genuinely actionable hourly swings in both
directions that cancel out", and Mark says as much in both reports. What had not been
appreciated is how far it goes: gross positive 3.14x the net, and the largest-magnitude
hour carrying the opposite sign to the day.

Also noted, with its caveat attached rather than as an assertion: **11:00 appears in four
of the five reports** that publish hourly concentration ($0.66, $1.10, ~$1.15, $2.45), and
the reference install's own largest-magnitude hour on 19 Sep is also 11:00 — opposite
sign. Two of his four attribute it to a real household load spike, which is a plausible
local explanation each time and not obviously the same thing as a cross-install pattern.
Flagged as a coincidence worth checking, with the discriminating question left to the one
install that can answer it: does 11:00 decompose into an energy difference or a price one?

---

## #1149 — the gap that made that day unreadable, shipped as v0.94.398

Every figure in the second table came from hand-summing 24 `j_ach_hourly`/`j_star_hourly`
rows, because the quality report publishes **no oracle-side energy totals and no grid
figures for any trajectory**. `fleet_achieved_energy_in_kwh`/`_out_kwh` give the achieved
half; there has never been a counterpart to difference it against.

`energy_decomposition`: four rows (`reference`, `achieved`, `oracle`,
`achieved_minus_oracle`), each `charge_kwh` + `discharge_kwh` + `grid_import_kwh` +
`grid_export_kwh`, built from arrays already in hand where the hourly rows are assembled,
so no extra solve.

Three properties that each earned a test:

- **The achieved row reconciles with the two existing `fleet_achieved_energy_*` fields by
  construction** — same arrays, same hours. Two independently-derived answers to one
  question is drift waiting to happen, so it is asserted rather than assumed.
- **Charge/discharge summed per battery as magnitudes, not from the fleet net.** A fleet
  where one battery charges while another discharges nets those against each other —
  right for a grid identity, wrong for "how much energy moved".
- **The delta row checked against its own two source rows**, not an expected constant.

A real bug the tests caught, fixed in the source rather than the test: `sum()` over an
empty generator returns `int` `0`, so the `reference` row — which passes empty battery
lists by construction — published ints where every sibling row published floats.

The two reconciliation tests and the delta test were **mutation-checked**: pointing the
achieved row at the oracle's arrays fails exactly those three and restoring turns them
green.

---

## A fifth correction, and it is the same species as the four above

The day's earlier entry records four corrections to its own work, all one species: a claim
that looked measured and was an artifact of how it was looked at. Here is a fifth, and it
cost real time.

**I killed two healthy test runs on bad arithmetic.** Watching the full local suite, a
process timestamp read as 10:56 was compared against a 14:01 clock and called "3h05m
elapsed, 27s CPU — hung". The machine is on **UTC+7** (the household is travelling); the
clock it was compared against was **UTC+10**. The run had been going about six minutes.
Killed it. Restarted, killed again on a variant of the same reasoning.

Three things worth keeping from that:

1. **The timezone rule this project already has is not only about what gets shown to a
   reader.** It is written as a presentation rule — always convert to AEST before speaking
   a time. This was arithmetic *on* two clocks in different zones, which that phrasing
   does not obviously cover. It should.
2. **A single CPU sample cannot distinguish slow from hung.** Two samples 2.7 minutes
   apart with CPU frozen at 103s is evidence; one sample plus a subtraction is not. The
   second diagnosis was right *and* the first was wrong, from the same kind of observation.
3. **A killed background task still reports `exit code 0` with an empty output file.**
   Twice. That is indistinguishable from a pass to anything reading only the exit code, and
   it was deliberately not counted as one — the same "a check that cannot fail is
   indistinguishable from a check that works" rule (#757) this day's earlier entry already
   records twice.

The suite does pass locally. It takes **18m31s** here: 3314 passed, 15 skipped, 1 xfailed,
506 subtests, zero failures. CI runs the same suite in 3m28s, which is a large enough gap
that the local number looked wrong — which is exactly what made it misreadable.

---

## Still open at end of session

- **#1120 is closed**, on a real verification rather than on the merge: `rescore_history`
  was called against the dev install's own quality sensor, bounds enforced,
  `latest_date_rescored: true`, the row stamped `v: "0.94.397"`, and the figures
  reproduced that install's independently-computed values to four decimals. The earlier
  section above was written before that happened and says it stays open — it does not.
- **#1149 is closed on the shipped mechanism, not on a validated day.** The oracle-side
  and grid figures now exist and are tested. Whether a real day's `achieved_minus_oracle`
  reads sensibly needs a day *scored under v0.94.398*, which by definition follows the
  release. If a scored day turns up something wrong with the figures themselves, that is a
  new, narrower issue rather than this one reopening.
- **The 11:00 question is genuinely open and is Mark's to answer**, because it needs his
  install's data: does that hour's regret decompose into an achieved-vs-oracle *energy*
  difference, or a price one? Four of his five reports name it; the reference install's own
  largest-magnitude hour on 19 Sep is also 11:00 but with the opposite sign. One
  phenomenon or two is not decidable from published figures.
- **#773** — untouched today, deliberately. The 2026-09-18 handover's warning stands: this
  issue has a long history of tidy explanations that did not survive measurement, and the
  warm-start proposal on #999 should not ship.
- The decisions owed by the household (#485, #1131, #1067, #949, #944, #987) remain owed,
  and nobody should start them unasked.
