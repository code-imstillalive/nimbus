# 2026-09-07

Long session, mostly dispatch-card dashboard work driven by direct household requests,
plus a few real bug fixes and the start of Mark Purcell's controllable-loads spec
(#476/#477-486). Version reaches v0.94.150, seven releases (v0.94.144 → v0.94.150).

## Real bug fixes
- **#445** — SoC discrepancy bound fixed.
- **#453** — live solar-override at period 0 was hardcoded `/1000.0` regardless of the
  sensor's real `unit_of_measurement`; fixed via the existing `_kw_scale_factor()` helper
  (already used elsewhere) instead of a second hardcoded conversion.
- **#451** — `build_tiered_grid()` reshaped: tier1 now boundary-snapped to the real
  current+next NEM 30-min trading interval (was a fixed 24h span); tier2 now 30-min
  resolution (was 1h), matching AEMO PD7DAY's real cadence. New constants
  `TRADING_INTERVAL_MINUTES=30`, `TIER2_PERIOD_HOURS=0.5`, `TOTAL_HORIZON_HOURS=96.0`,
  `MAX_TIER1_HOURS=1.0`. 30 new/updated tests, including hand-derived tier1-boundary math
  that caught two small arithmetic slips in the issue's own prose.
- `SharedCircuitConfig` built and shipped.

## Dashboard: risk-aversion proof, Solve Now, status sensor, link fix
- Added `solar_risk_effect_now_kw` / `import_price_risk_effect_now` /
  `export_price_risk_effect_now` on `sensor.nimbus_solver_battery_forecast`
  (`_risk_aversion_effect_now()` in `solver_writer.py`, using new `Plan.effective_*`
  fields in `network.py`) plus a "live effect this cycle" readout under each of the three
  risk sliders on the dispatch card — real, live-verified nonzero effect after a genuine
  slider change (confirmed on NUC1).
- Added a **Solve Now** button on the dispatch card calling the real
  `nimbus_load.solve_now` service, per a phone conversation between the household and
  Mark — simplification he suggested directly.
- Added `sensor.nimbus_status` (#450) — plain-language "Working well"/"Learning"/"Needs
  attention"/"Not yet configured" status ahead of the 13+ jargon sensors, pure
  presentation layer reading existing sensors' live states.
- Fixed the "Full Solver settings" link — was hardcoded to a devhub-only
  `/nimbus-devhub/solver` path, silently falling through to HAEO's own dashboard on any
  other install. Now `/config/integrations/integration/nimbus_load`, HA's universal path.
- Fixed table row density (#456), table stretching wide (#459).

## Dashboard: chart/table layout saga (v0.94.144 → v0.94.150)
Direct household ask, from an annotated screenshot: chart-left-2/3 + table-right-1/3,
side by side, narrower TIME/SOURCE columns, taller chart. Landed across several releases,
each genuinely shipped and CI-verified, but reported still broken on the actual page after
every one — "TABLE STILL SCROLLING - SOURCE STILL NOT FIXED, PLAN STILL NOT FULL HEIGHT -
EMBARRASING", then still broken after a full browser restart and a fresh incognito window.

Every environment theory was tested and ruled out directly: browser disk cache (ruled out
by curl on NUC1 confirming the exact newest CSS markers were being served, and a DevTools
Network capture showing genuine fresh 200 fetches, not disk-cache hits), the HA frontend
service worker, custom-element redefinition surviving across reloads (ruled out by a full
app restart + a separate incognito test, both still broken).

**Root cause, found by re-reading the actual CSS rather than guessing another pixel value**:
the 2/3+1/3 split used a fixed CSS grid fraction (`2fr 1fr`) gated by a hand-computed
`@container` breakpoint (1708px, derived from `--ftable-min-width * 3 + gap`). This is the
same failure class as #391/#395/#400/#407 (three earlier breakpoint mismatches this project
already hit) — giving the table column a fixed *fraction* of the row caps its width
regardless of what the table's real content needs, so no hand-picked breakpoint can stay
correct once anything about the table's own real minimum width shifts (row padding, font
size, a header's own text width). Three same-day tweaks to bar width, `--ftable-min-width`,
and the SOURCE header's own font/letter-spacing all genuinely reduced the table's real
minimum width, but none of them could reliably close the gap against an independently-fixed
1/3 share of the row.

**Fix (v0.94.150)**: replaced the grid+breakpoint entirely with flexbox. `table-col` is
`flex: 0 0 auto` — always exactly its own real content width, no floor needed. `chart-col`
is `flex: 1 1 480px; min-width: 320px` — takes whatever space is left, never below its own
floor. `flex-wrap: wrap` drops `table-col` onto its own full-width line if the two
genuinely don't both fit. There is no breakpoint number left to keep in sync with the
table's real width — the table can only scroll horizontally now when the viewport itself
is too narrow for its real content, never from an arbitrary fraction being smaller than
that content by construction. Not yet confirmed fixed live by the household as of this
entry — deployed to devhub, NUC1 instructions given, live confirmation pending.

Also (same saga): dispatch-plan chart clipped to 3 days / 72h instead of the full 96h
forecast (chart-only — the table and other readers still see the full forecast), and the
chart's own SVG `viewBox` height raised 220 → 275 (+25%) per direct request. An earlier,
more complex flex-stretch + `preserveAspectRatio="none"` approach to make the chart
literally fill the table's height was built and then fully reverted (`git checkout --`)
after a household simplification request — not revisited this session.

## #476/#477 — controllable-loads spec (Mark Purcell), started not finished
Mark filed a 10-part spec (#476 umbrella + #477-486), researched against EMHASS/HAEO
source, with a concrete design: every requirement is priced slack, not hard-constrained;
"run when cheap" falls out of the LP's own switchboard shadow price via `value_per_kwh`;
pure LP by default, binaries opt-in (#478). Delivery order per Mark: #477 (foundation) →
#486 (config surface) → #483/#484 (costing/monitoring) → #480/#479/#482 → #481 (thermal,
largest) → #478 (binaries) → #485 (modes).

Started #477: `elements.py`'s `AdequacyLoadConfig` fully rewritten (new `shortfall_price`
required field replacing the old hard deadline constraint, new optional `allowed` per-period
mask, `value_per_kwh` utility credit, `max_cost_per_run` reserved for #478). **Not yet
applied to `network.py`** — the LP-side variable construction, shortfall slack variable,
cost wiring, and deadline-constraint rewrite are all still pending, along with updating the
9 existing `AdequacyLoadConfig(...)` test call sites. Paused mid-flight (stashed, not lost)
when the dashboard escalation above took priority for the rest of the session — resume from
the stash next session.

## Multi-battery/EV (#467) — still not started
Household scoping decision recorded as a GitHub comment only (bidirectional EV = just
another battery, optional future "is this an EV" flag on `BatteryConfig`, single-direction
EV stays a `LoadConfig`). Zero code written.
